import React, { Component } from 'react';
import './style.css';

class Header extends Component() {
  Render() {
    return(
      <header>

      </header>
    );
  }
}
 export default Header;